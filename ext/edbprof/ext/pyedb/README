PyEDB: Python interface for controlling EDB
===========================================

PyEDB depends on the interface definition in `edb.json`, which is generated by
`scripts/gen-interface.py` in the EDB repo.
